var http = require('http');
var fs = require('fs');
var formatable = require('formatable');
http.createServer(function(req,res))
{

    if(req.url =='/'

    {
        res.writeHead()
    })
}
